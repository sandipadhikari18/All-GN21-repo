<Window x:Class="Assignment_2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Assignment_2"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="900">
    <DockPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250*" MinWidth="150"/>
                <ColumnDefinition Width="500*" MinWidth="300"/>
                <ColumnDefinition Width="300*" MinWidth="200"/>
            </Grid.ColumnDefinitions>
            <local:ResearcherListView x:Name="ResearcherList"/>
            <local:ResearcherDetailsView x:Name="ResearcherDetails" Grid.Column="1"/>
            <local:PublicationDetailsView x:Name="PublicationDetails" Grid.Column="2"/>
        </Grid>
    </DockPanel>
</Window>
    <<!--Here, everything needs to be connected but it isnot connecting this way-->

    